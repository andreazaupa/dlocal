<%= form_for(@organization,:html=>{:multipart=>true}) do |f| %>
  <% if @organization.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@organization.errors.count, "error") %> prohibited this organization from being saved:</h2>

      <ul>
      <% @organization.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  
  <div class="field">
   <%= f.label :archivio %><br />
     <%= f.file_field :asset %>
  </div>
  
  <!-- <div class="field">
    <%= f.label :locales %><br />
    <%= f.text_field :locales %>
  </div> -->
  <%= f.hidden_field :locales, "it" %>
  <!-- <div class="field">
     <%= f.label :active %><br />
     <%= f.check_box :active %>
   </div> -->
  <div class="actions">
    <%= f.submit ,:class=>"button"%>
  </div>
<% end %>
